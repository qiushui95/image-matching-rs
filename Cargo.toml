[package]
name = "image-matching-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.24", features = ["png"] }
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
